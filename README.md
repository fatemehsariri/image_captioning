## Image Captioning using CNN + Transformer

This project generates natural language descriptions for images using:
- ResNet50 for feature extraction
- Transformer Decoder for sequence generation
- Flickr30k dataset

### Architecture
ResNet50 → Feature Vector → Transformer Decoder → Caption

### Example Output
Input: image of a dog
Output: "a dog is running in the park"
